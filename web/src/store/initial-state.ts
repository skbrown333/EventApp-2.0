import { MAP } from "../constants/constants";

const initialState = {
  account: {},
  current_location: {},
  center: MAP.default_location,
  zoom: 15,
  // budgets: [],
  // transactions: [],
  toast: []
};

export default initialState;
